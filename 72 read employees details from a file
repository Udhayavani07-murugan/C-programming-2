#include <stdio.h>

struct Employee {
    int id;
    char name[50];
    float salary;
};

int main() {
    struct Employee e;
    FILE *fp;

    fp = fopen("employees.txt", "r"); // Open file in read mode
    if (fp == NULL) {
        printf("Error: Cannot open file.\n");
        return 1;
    }

    printf("Employee Details:\n");
    while (fscanf(fp, "ID: %d, Name: %49[^,], Salary: %f\n", &e.id, e.name, &e.salary) != EOF) {
        printf("ID: %d, Name: %s, Salary: %.2f\n", e.id, e.name, e.salary);
    }

    fclose(fp);
    return 0;
}
