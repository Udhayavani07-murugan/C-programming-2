#include <stdio.h>

struct Student {
    int roll;
    char name[20];
};

int main() {
    int n;

    printf("Enter number of records: ");
    scanf("%d", &n);

    printf("Total Records = %d\n", n);
    return 0;
}
